{
  "name": "signal-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Node.js portfolio starter for Coolify",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check dist/content.js && node --check dist/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
